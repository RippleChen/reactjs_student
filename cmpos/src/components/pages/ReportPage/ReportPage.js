import React from "react";
import { Line } from "react-chartjs-2";

const ReportPage = () => {
  return (
    <div style={{ marginTop: 100 }}>
      <Line data={} height={100} width={500} />
    </div>
  );
};

export default ReportPage;
